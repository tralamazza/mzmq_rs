{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"# Available Homebrew Packages. The following CLI tools and libraries are installed on this system: $(brew list --formula -1)\"",
            "timeout": 10000
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_TOOL_INPUT_FILE_PATH\" | grep -q '\\.rs$' && cargo clippy --all-targets --all-features -- -D warnings 2>&1 | head -50 || exit 0"
          }
        ]
      }
    ]
  }
}
